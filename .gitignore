# Dependencies
node_modules/

# Live browser session (cookies = account access) — NEVER commit
storage/

# Note: users.txt IS committed (as a placeholder). Your real accounts go in it
# locally; that copy is protected from accidental commits via:
#   git update-index --skip-worktree users.txt

# Logs (may contain account activity)
*.log

# Secrets / local env
.env
.env.*
*.local

.DS_Store
