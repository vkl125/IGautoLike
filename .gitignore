# Dependencies
node_modules/

# Live browser session (cookies = account access) — NEVER commit
storage/

# Your real target list (personal data) — git-ignored so it never publishes.
# Fresh clones copy users.example.txt -> users.txt.
users.txt

# Logs (may contain account activity)
*.log

# Secrets / local env
.env
.env.*
*.local

.DS_Store
